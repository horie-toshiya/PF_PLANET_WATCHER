<h4>掲示板一覧</h4>
 <table class="table table-hover table-inverse">
      <thead>
        <tr>
          <th>投稿ID</th>
          <th>投稿時間</th>
          <th>タイトル</th>
          <th>投稿者</th>
        </tr>
      </thead>
     <tbody>
       <% @boards.each do |board| %>
         <tr>
　　　　　<td><%= board.id %></td>
　　　　　<td><%= board.created_at %></td>
　　　　　<td><%= link_to board.title,admin_board_path(board.id) %></td>
　　　　　<td><%= board.name %></td>
　　　　 </tr>
       <% end %>
     </tbody>
  </table>